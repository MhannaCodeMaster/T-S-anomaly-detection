mode: train

dataset:
  root: /kaggle/input/mvtec-ad
  category: leather

student_training:
  epochs: 100
  batch_size: 32

triplet_training:
  epochs: 100
  batch_size: 32

heatmap_threshold:
  method: percentile
  value: 95.0

models:
  st_path: null
  tl_path: null